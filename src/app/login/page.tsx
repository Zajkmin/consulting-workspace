"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{useApp}from"@/hooks/use-app";
export default function LoginPage(){const{login}=useApp(),router=useRouter(),[email,setEmail]=useState("jazmin@consulting-workspace.test"),[password,setPassword]=useState("consulting123"),[error,setError]=useState("");return <main className="login-page"><section className="login-brand"><div><span className="brand-mark large">G</span><p className="eyebrow">Gestión de consultoría</p><h1>El trabajo importante,<br/>en una sola dirección.</h1><p>Organizá proyectos, prioridades y tiempo sin perder de vista a tus clientes.</p></div><small>Espacio de trabajo para equipos de consultoría</small></section><section className="login-panel"><form className="login-form" onSubmit={e=>{e.preventDefault();if(login(email,password)){router.replace("/")}else setError("El correo o la contraseña no son correctos.")}}><div><p className="eyebrow">Bienvenido</p><h2>Iniciar sesión</h2><p>Ingresá con tu cuenta para ver los proyectos que tenés asignados.</p></div><label>Correo electrónico<input autoFocus type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="nombre@consulting-workspace.test"/></label><label>Contraseña<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="form-error">{error}</p>}<button className="button primary login-button">Ingresar</button><div className="demo-access"><b>Acceso de demostración</b><span>Administrador: jazmin@consulting-workspace.test</span><span>Consultor: romina@consulting-workspace.test</span><span>Contraseña: consulting123</span></div></form></section></main>}
