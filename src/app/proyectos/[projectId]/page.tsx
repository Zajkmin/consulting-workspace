"use client";
import Link from"next/link";import{useParams}from"next/navigation";import{useApp}from"@/hooks/use-app";import{Hierarchy}from"@/components/initiatives/hierarchy";export default function ProjectPage(){const{projectId}=useParams<{projectId:string}>(),{data}=useApp(),project=data.projects.find(p=>p.id===projectId);if(!project)return <main className="shell"><div className="empty"><b>Proyecto no encontrado</b><Link href="/">Volver a iniciativas</Link></div></main>;return <main className="shell"><nav className="crumb"><Link href="/">Proyectos</Link><span>/</span><span>{project.name}</span></nav><section className="project-head"><div><p className="eyebrow">Proyecto</p><h1>{project.name}</h1><p className="subtle">Seguimiento del compromiso general, sus versiones entregables y el trabajo concreto.</p></div></section><Hierarchy projectId={project.id}/></main>}
