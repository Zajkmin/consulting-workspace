"use client";
import{useState}from"react";import{useApp}from"@/hooks/use-app";export function AreaForm({projectId,onDone}:{projectId:string;onDone:()=>void}){const{addArea}=useApp(),[name,setName]=useState("");return <form onSubmit={e=>{e.preventDefault();if(!name.trim())return;addArea(projectId,name.trim());onDone()}}><label>Nombre del área<input autoFocus required value={name} onChange={e=>setName(e.target.value)} placeholder="Ej. Operaciones"/></label><div className="modal-actions"><button type="button" className="button quiet" onClick={onDone}>Cancelar</button><button className="button primary">Crear área</button></div></form>}
