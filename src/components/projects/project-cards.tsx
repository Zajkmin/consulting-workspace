import Link from"next/link";import type{Project,Task}from"@/types";import{projectStats}from"@/lib/format";export function ProjectCards({projects,tasks}:{projects:Project[];tasks:Task[]}){return <div className="project-grid">{projects.map(p=>{const s=projectStats(p.id,tasks);return <Link key={p.id} href={`/proyectos/${p.id}`} className="project-card"><i style={{background:p.color}}/><div className="project-title"><b>{p.name}</b><span>→</span></div><small>{s.completed+s.pending+s.late} tareas activas</small><div className="project-stats"><span><b>{s.completed}</b> completadas</span><span><b>{s.pending}</b> pendientes</span><span className="late"><b>{s.late}</b> retrasadas</span></div></Link>})}</div>}
