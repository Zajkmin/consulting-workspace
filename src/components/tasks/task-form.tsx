"use client";
import{useState}from"react";import{useApp}from"@/hooks/use-app";import type{Task}from"@/types";
export function TaskForm({projectId,initiativeId,versionId,onDone}:{projectId:string;initiativeId:string;versionId:string;onDone:()=>void}){const{data,allData,addTask}=useApp(),[title,setTitle]=useState(""),[description,setDescription]=useState(""),[priority,setPriority]=useState<Task["priority"]>("Media"),[deadline,setDeadline]=useState("2026-09-18"),[minutes,setMinutes]=useState(60),[assignedTo,setAssignedTo]=useState(data.user.name),[splittable,setSplittable]=useState(true);return <form onSubmit={e=>{e.preventDefault();addTask({id:`t-${Date.now()}`,projectId,initiativeId,versionId,title,description,priority,status:"Pendiente",deadline,estimatedMinutes:minutes,splittable,progress:0,assignedTo,dependencies:[],subtasks:[]});onDone()}}><div className="form-grid"><label className="full">Tarea<input autoFocus required value={title} onChange={e=>setTitle(e.target.value)} placeholder="¿Qué hay que hacer?"/></label><label>Prioridad<select value={priority} onChange={e=>setPriority(e.target.value as Task["priority"])}><option>Alta</option><option>Media</option><option>Baja</option></select></label><label>Responsable<select value={assignedTo} onChange={e=>setAssignedTo(e.target.value)}>{allData.users.filter(u=>u.active&&(u.role==="admin"||u.assignedProjectIds.includes(projectId))).map(u=><option key={u.id} value={u.name}>{u.name}</option>)}</select></label><label>Fecha límite<input type="date" value={deadline} onChange={e=>setDeadline(e.target.value)}/></label><label>Duración estimada<input type="number" min="15" step="15" value={minutes} onChange={e=>setMinutes(Number(e.target.value))}/></label><label className="full">Descripción<textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Contexto, resultado esperado o criterios de finalización"/></label><label className="full checkbox-label"><input type="checkbox" checked={splittable} onChange={e=>setSplittable(e.target.checked)}/> Puede dividirse en varios bloques de agenda</label></div><div className="modal-actions"><button type="button" className="button quiet" onClick={onDone}>Cancelar</button><button className="button primary">Crear tarea</button></div></form>}
