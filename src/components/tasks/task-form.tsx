"use client";
import{useState}from"react";import{useApp}from"@/hooks/use-app";import type{Task}from"@/types";
export function TaskForm({projectId,initiativeId,versionId,onDone}:{projectId:string;initiativeId:string;versionId:string;onDone:()=>void}){const{data,allData,addTask}=useApp(),version=data.versions.find(item=>item.id===versionId),[title,setTitle]=useState(""),[description,setDescription]=useState(""),[priority,setPriority]=useState<Task["priority"]>("Media"),[assignedTo,setAssignedTo]=useState(data.user.name);return <form onSubmit={e=>{e.preventDefault();addTask({id:crypto.randomUUID(),projectId,initiativeId,versionId,title,description,priority,status:"Pendiente",deadline:version?.deadline??"",estimatedMinutes:0,splittable:true,progress:0,assignedTo,dependencies:[],subtasks:[]});onDone()}}><div className="form-grid"><label className="full">Tarea<input autoFocus required value={title} onChange={e=>setTitle(e.target.value)} placeholder="¿Qué hay que hacer?"/></label><label>Prioridad<select value={priority} onChange={e=>setPriority(e.target.value as Task["priority"])}><option>Alta</option><option>Media</option><option>Baja</option></select></label><label>Responsable<select value={assignedTo} onChange={e=>setAssignedTo(e.target.value)}>{allData.users.filter(u=>u.active&&(u.role==="admin"||u.assignedProjectIds.includes(projectId))).map(u=><option key={u.id} value={u.name}>{u.name}</option>)}</select></label><label className="full">Descripción<textarea className="auto-grow" rows={1} value={description} onChange={e=>setDescription(e.target.value)} placeholder="Contexto o resultado esperado"/></label></div><div className="modal-actions"><button type="button" className="button quiet" onClick={onDone}>Cancelar</button><button className="button primary">Crear tarea</button></div></form>}
